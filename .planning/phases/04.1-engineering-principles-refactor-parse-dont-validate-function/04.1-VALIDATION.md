---
phase: 04.1
slug: engineering-principles-refactor-parse-dont-validate-function
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-22
---

# Phase 04.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest (configured in vite.config.ts) |
| **Config file** | `apps/web/vite.config.ts` — `test.projects` array with `api` (node) and `frontend` (jsdom) environments |
| **Quick run command** | `cd apps/web && npx vitest run api/_lib/db/tasks.test.ts` |
| **Full suite command** | `cd apps/web && npm test` |
| **Estimated runtime** | ~10 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd apps/web && npx vitest run <relevant-test-file>`
- **After every plan wave:** Run `cd apps/web && npm test`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~10 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| Wave 0 — tasks.test.ts stubs | TBD | 0 | D-01, D-08 | T-V5 | `rowToTask()` throws ZodError on bad status | unit | `cd apps/web && npx vitest run api/_lib/db/tasks.test.ts` | ❌ W0 | ⬜ pending |
| Wave 0 — products.test.ts stubs | TBD | 0 | D-01 | — | `rowToProduct()` throws ZodError on bad row | unit | `cd apps/web && npx vitest run api/_lib/db/products.test.ts` | ❌ W0 | ⬜ pending |
| D-01 DB parse | TBD | 1 | D-01 | T-V5 | `rowToTask()` assembles correct Task variant for all 8 statuses | unit | `cd apps/web && npx vitest run api/_lib/db/tasks.test.ts` | ❌ W0 | ⬜ pending |
| D-01 GitHub parse | TBD | 2 | D-01 | T-V5 | `mapGitHubIssue()` throws ZodError on missing required field | unit | `cd apps/web && npx vitest run api/_lib/github.test.ts` | ✅ (extend) | ⬜ pending |
| D-01 GitHub parse | TBD | 2 | D-01 | T-V5 | `mapGitHubPR()` throws ZodError on missing required field | unit | `cd apps/web && npx vitest run api/_lib/github.test.ts` | ✅ (extend) | ⬜ pending |
| D-04 pure functions | TBD | 3 | D-04 | — | `determineRole(0)` returns 'admin'; `determineRole(1)` returns 'member' | unit | `cd apps/web && npx vitest run api/auth/github/callback.test.ts` | ✅ (extend) | ⬜ pending |
| D-04 pure functions | TBD | 3 | D-04 | — | `buildEmailFallback(null, 'alice')` returns `alice@github.invalid` | unit | `cd apps/web && npx vitest run api/auth/github/callback.test.ts` | ✅ (extend) | ⬜ pending |
| D-08 Task FSM | TBD | 1 | D-08, D-09 | T-V5 | `rowToTask()` with `status='error'` and no `review_reason` fails Zod parse | unit | `cd apps/web && npx vitest run api/_lib/db/tasks.test.ts` | ❌ W0 | ⬜ pending |
| D-08 OAuth FSM | TBD | 3 | D-08 | T-V2 | `validateOAuthState()` returns false for mismatched states | unit | `cd apps/web && npx vitest run api/auth/github/callback.test.ts` | ✅ (extend) | ⬜ pending |
| D-08 Triage FSM | TBD | 1 | D-08 | — | `rowToTriage()` maps `is_triaged=1, priority=null` to `{ kind: 'complete' }` | unit | `cd apps/web && npx vitest run api/_lib/db/triage.test.ts` | ✅ (extend) | ⬜ pending |
| Regression | TBD | All | All | — | All 6 existing test files pass | regression | `cd apps/web && npm test` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `apps/web/api/_lib/db/tasks.test.ts` — stubs for `rowToTask()` Zod parse + discriminated union assembly (D-01, D-08, D-09)
- [ ] `apps/web/api/_lib/db/products.test.ts` — stubs for `rowToProduct()` Zod parse (D-01, optional if products.ts not fully refactored)

*All other test gaps are extensions to existing files, not new files.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| TypeScript compilation passes after discriminated union change | D-09 | Compiler-only check | `cd apps/web && npx tsc --noEmit` |
| All existing DB rows have valid `status` values before Zod migration | D-01 | Requires DB access | Run: `SELECT DISTINCT status FROM tasks;` — confirm all 8 known values present |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
