---
phase: "07"
plan: "04"
type: execute
wave: 3
depends_on:
  - "07-02"
  - "07-03"
files_modified: []
autonomous: false
requirements:
  - PROMOTE-01
  - PROMOTE-02
  - PROMOTE-03
  - PROMOTE-04
  - PROMOTE-05

must_haves:
  truths:
    - "User can click Promote to Backlog on an unpromoted issue and see it appear in the Kanban backlog"
    - "Promoting the same issue a second time shows the duplicate toast and badge instead of creating a second task"
    - "View in Backlog badge is present after promotion and clicking it navigates to the Kanban board"
    - "Editing the promoted task title in Currents writes back to the GitHub issue (PROMOTE-02)"
    - "Full automated suite passes green before checkpoint"
  artifacts:
    - path: "apps/web/api/tasks/by-github-issue.ts"
      provides: "Deployed GET endpoint"
    - path: "apps/web/src/client/components/IssueDetailPanel.tsx"
      provides: "Promote feature live in browser"
  key_links:
    - from: "IssueDetailPanel (browser)"
      to: "POST /api/tasks"
      via: "promote button click → promoteMutation → HTTP POST"
      pattern: "manual smoke test"
    - from: "PATCH /api/tasks/:id"
      to: "syncTaskToGitHub()"
      via: "automatic write-back on task edit"
      pattern: "manual smoke test"
---

<objective>
Wave 3 — Human verification. Smoke-test the full promote flow in the running application.

Purpose: Confirm that the implemented feature works end-to-end in a real browser with real data — automated tests cover component logic but cannot verify the full round-trip (create task → Kanban appears → edit → GitHub write-back).

Output: Human confirms all 5 promote requirements work in browser. Phase 7 is complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-to-use-github-oauth-login-to-use-the-given-repo-permissions-/07-02-SUMMARY.md
@.planning/phases/07-to-use-github-oauth-login-to-use-the-given-repo-permissions-/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated suite gate before smoke test</name>
  <files></files>

  <read_first>
    - No file reads needed — this is a verification-only task
  </read_first>

  <action>
    Run the full test suite and typecheck to confirm Wave 1 and Wave 2 are fully green before asking the user to smoke test:

    ```bash
    cd apps/web && npm test && npm run typecheck
    ```

    If any tests fail, do NOT proceed to the human checkpoint. Fix the failing tests first (trace back to the appropriate Wave 1 or Wave 2 plan).
  </action>

  <verify>
    <automated>cd /Users/ivanfoong/repos/dxd/Currents/apps/web && npm test && npm run typecheck 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `npm test` exits 0 — all tests GREEN including PROMOTE-01/03/04/05
    - `npm run typecheck` exits 0 — no TypeScript errors
    - Test output contains passing PROMOTE-01, PROMOTE-03, PROMOTE-04, PROMOTE-05 describe blocks
  </acceptance_criteria>

  <done>
    Automated suite exits 0. Safe to proceed to human verification.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The "Promote to Backlog" feature has been fully implemented:
    - Promote button in IssueDetailPanel fires POST /api/tasks to create a backlog task
    - Already-promoted issues show a "View in Backlog" badge instead of the button
    - Triage priority pre-populates the task priority (critical → urgent)
    - Duplicate promotion returns 409, shows toast error and the badge
    - Write-back: editing the task in Currents writes back to GitHub via existing syncTaskToGitHub
    - i18n: both EN and FR locale files have all 7 "promote" keys
  </what-built>

  <how-to-verify>
    Start the dev server: `cd apps/web && npx tsx scripts/dev-server.ts`

    **Smoke test 1 — Happy path (PROMOTE-01, PROMOTE-04):**
    1. Log in via GitHub OAuth
    2. Navigate to a product that has a connected GitHub repo
    3. Open the Issues tab and click any open issue
    4. Confirm: "Promote to Backlog" button is visible below the triage controls
    5. If the issue has a triage priority set (e.g., "High"), note the priority
    6. Click "Promote to Backlog"
    7. Confirm: success toast "Issue promoted to backlog." appears
    8. Confirm: button is replaced by "View in Backlog" badge
    9. Navigate to the Kanban board (`/products/:productId`)
    10. Confirm: the new task appears in the Backlog column with the correct title and priority (critical → urgent if applicable)

    **Smoke test 2 — Duplicate guard (PROMOTE-05):**
    1. Open the same issue panel again (the one just promoted)
    2. Confirm: "View in Backlog" badge is shown (not the promote button) — because existingTask query found it
    3. Optional: If testing fresh load, navigate away and back to confirm badge persists on reload

    **Smoke test 3 — Write-back (PROMOTE-02):**
    1. Click "View in Backlog" to navigate to the Kanban board
    2. Find the promoted task in the Backlog column and edit its title (e.g., add "[EDITED]")
    3. Save the edit (PATCH /api/tasks/:id fires automatically)
    4. Wait 2-3 seconds, then check the GitHub issue on github.com
    5. Confirm: the GitHub issue title now reflects the edit (syncTaskToGitHub fired)

    **Smoke test 4 — French locale (PROMOTE i18n):**
    1. Change language to French in settings (if available) or check fr/issues.json exists with "promote" keys
    2. Confirm: French translations appear if FR locale is active
  </how-to-verify>

  <resume-signal>
    Type "approved" if all smoke tests pass.
    Or describe any issues found (e.g., "badge doesn't appear after promote", "write-back not firing") so they can be fixed.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human → Running app | User authenticates and interacts with live application; no new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Elevation of Privilege | Human smoke test | accept | Dev server uses MOCK_SERVICES — no real production data; test is isolated to dev.db |
</threat_model>

<verification>
Phase 7 is complete when:
1. `cd apps/web && npm test && npm run typecheck` exits 0
2. Human verifies promote happy path, duplicate guard, and write-back in browser
3. Phase 7 summary created at `.planning/phases/07-to-use-github-oauth-login-to-use-the-given-repo-permissions-/07-04-SUMMARY.md`
</verification>

<success_criteria>
All 5 PROMOTE requirements verified end-to-end:
- PROMOTE-01: One-click promote from triage panel ✓
- PROMOTE-02: Write-back fires on task edit (automatic via existing sync) ✓
- PROMOTE-03: View in Backlog badge shows when already promoted ✓
- PROMOTE-04: Triage priority pre-populates task priority ✓
- PROMOTE-05: Duplicate blocked with clear message ✓
</success_criteria>

<output>
After completion, create `.planning/phases/07-to-use-github-oauth-login-to-use-the-given-repo-permissions-/07-04-SUMMARY.md`
</output>
