---
phase: 04.2-fake-data-and-mocked-services-for-dev-env
verified: 2026-04-22T12:00:00Z
status: human_needed
score: 9/10 must-haves verified
overrides_applied: 0
overrides:
  - must_have: "vite.config.ts define block maps import.meta.env.VITE_MOCK_SERVICES from process.env.MOCK_SERVICES"
    reason: "Plan 05 intentionally removed the define block after discovering it always resolved to 'false' because Vite loads .env.local after config evaluation. Vite's built-in VITE_* env handling is used instead — VITE_MOCK_SERVICES=true in .env.local is auto-exposed to import.meta.env. DevModeBanner still reads import.meta.env.VITE_MOCK_SERVICES correctly."
    accepted_by: "gsd-verifier (auto-detected intentional deviation)"
    accepted_at: "2026-04-22T12:00:00Z"
human_verification:
  - test: "Amber Dev Mode banner visible in browser"
    expected: "With VITE_MOCK_SERVICES=true in .env.local, amber 'Dev Mode' banner appears fixed top-right on both login and authenticated pages"
    why_human: "Visual UI rendering requires browser; cannot verify import.meta.env resolution or CSS without running Vite"
  - test: "Mock OAuth bypass works end-to-end"
    expected: "Clicking GitHub sign-in immediately redirects to /?token=... and logs in as Dev Admin without visiting github.com"
    why_human: "Requires running dev-server.ts and Vite simultaneously; verifying redirect flow needs a live browser session"
  - test: "Mock GitHub issues load in /issues view"
    expected: "Issues list at /issues shows mock fixture titles like 'Fix null pointer exception in auth flow' from all 3 seeded products"
    why_human: "Requires full stack running (dev-server + Vite + seeded dev.db); visual content verification"
  - test: "seed.ts runs cleanly when MOCK_SERVICES=true"
    expected: "Running 'cd apps/web && npx tsx scripts/seed.ts' with MOCK_SERVICES=true in .env.local exits 0 and logs [seed] lines for 1 user, 3 products, 24 tasks"
    why_human: "Requires file system write access to dev.db and correct .env.local setup; best verified by developer in their local environment"
---

# Phase 04.2: Fake Data and Mocked Services for Dev Env — Verification Report

**Phase Goal:** Provide a complete local dev environment with mocked external services (SQLite instead of Turso, mock GitHub API, mock OAuth) controlled by a single MOCK_SERVICES=true env var, with a deterministic seed script and a visible dev mode indicator.
**Verified:** 2026-04-22T12:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | getClient() uses file:dev.db when MOCK_SERVICES=true | VERIFIED | `client.ts` line 8–10: `if (process.env.MOCK_SERVICES === 'true') { client = createClient({ url: 'file:dev.db' }); }` — exact string match gate present |
| 2 | getClient() uses TURSO_DATABASE_URL when MOCK_SERVICES is unset | VERIFIED | `client.ts` lines 11–15: else branch uses `TURSO_DATABASE_URL` + `TURSO_AUTH_TOKEN` unchanged |
| 3 | seed.ts aborts with process.exit(1) when MOCK_SERVICES is not 'true' | VERIFIED | `seed.ts` lines 4–9: guard at top of file before any DB import; calls `process.exit(1)` with explanatory error messages |
| 4 | seed.ts creates deterministic data with faker.seed(12345) | VERIFIED | `seed.ts` line 15: `faker.seed(12345)` at module level before any faker calls |
| 5 | seed.ts creates 1 admin user, 3 products, tasks in all 8 Kanban statuses | VERIFIED | `seed.ts`: `ALL_STATUSES` array lists all 8 statuses; loop creates one task per status per product; `pr_created` tasks have GitHub fields; `ai_review/human_review/error` tasks have `review_reason` |
| 6 | buildIssueFixtures() and buildLabelFixtures() exported from github-fixtures.ts | VERIFIED | `github-fixtures.ts` lines 69 and 100: both are named exports; 20 issues (15 open + 5 closed), no `pull_request` field |
| 7 | dev-server.ts injects mock middleware BEFORE collectRoutes() when MOCK_SERVICES=true | VERIFIED | `dev-server.ts` lines 57–62: MOCK_SERVICES guard + `registerMockRoutes(app)` call appears before `collectRoutes(API_DIR)` at line 64 |
| 8 | DevModeBanner renders null when VITE_MOCK_SERVICES !== 'true', amber banner otherwise | VERIFIED | `DevModeBanner.tsx` lines 1–8: `if (import.meta.env.VITE_MOCK_SERVICES !== 'true') return null` guard; amber Tailwind classes present |
| 9 | DevModeBanner is mounted in App.tsx on both login and authenticated views | VERIFIED | `App.tsx` line 13: import present; line 67: `<DevModeBanner />` in unauthenticated return; line 139: `<DevModeBanner />` in `AuthenticatedApp` |
| 10 | vite.config.ts define block maps VITE_MOCK_SERVICES | PASSED (override) | Define block intentionally removed after discovering it evaluated before .env.local loaded (always resolved 'false'). Vite's native VITE_* handling used instead — functionally equivalent. DevModeBanner still reads `import.meta.env.VITE_MOCK_SERVICES`. |

**Score:** 9/10 automated truths verified (1 override applied for intentional deviation)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/web/api/_lib/db/client.ts` | MOCK_SERVICES conditional in getClient() | VERIFIED | Lines 8–10: exact gate present with Vercel safety check bonus |
| `apps/web/scripts/seed.ts` | Deterministic seeder with faker.seed(12345) | VERIFIED | Complete; all 8 statuses, idempotent DELETE-before-INSERT |
| `apps/web/scripts/mocks/github-fixtures.ts` | Fixture builders + mock Express routes | VERIFIED | 137 lines; `buildIssueFixtures`, `buildLabelFixtures`, `registerMockRoutes` all exported; 15 open + 5 closed issues; no `pull_request` field |
| `apps/web/scripts/dev-server.ts` | Conditional mock middleware injection | VERIFIED | Lines 57–62: dynamic import + `registerMockRoutes(app)` before `collectRoutes()` |
| `apps/web/src/client/components/DevModeBanner.tsx` | Dev mode indicator component | VERIFIED | 8 lines; null guard + amber banner |
| `apps/web/src/client/App.tsx` | DevModeBanner mounted in both branches | VERIFIED | Lines 13, 67, 139 |
| `apps/web/.env.example` | MOCK_SERVICES docs with production warning | VERIFIED | Lines 19–25: full documentation block including "NEVER set MOCK_SERVICES=true in production" |
| `apps/web/package.json` | @faker-js/faker devDependency | VERIFIED | Line 49: `"@faker-js/faker": "^10.4.0"` in devDependencies |
| `apps/web/vite.config.ts` | scripts test project (scripts/**/*.test.ts) | VERIFIED | Lines 48–57: third project entry with name 'scripts' and include pattern |
| `CLAUDE.md` | Engineering principle 5 — Mocked Services | VERIFIED | Lines 546–597: full principle section with setup instructions, service table, CORRECT/WRONG examples, and production warning |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `api/_lib/db/client.ts` | `file:dev.db` (local SQLite) | `createClient({ url: 'file:dev.db' })` | WIRED | Conditional branch confirmed in code |
| `scripts/dev-server.ts` | `scripts/mocks/github-fixtures.ts` | `import('./mocks/github-fixtures.js')` → `registerMockRoutes(app)` | WIRED | Dynamic import at line 59; called at line 60 |
| `scripts/mocks/github-fixtures.ts` | `api/_lib/auth/jwt.ts` | `createToken(user.id, user.email, user.role)` | WIRED | Import at line 2; called in OAuth callback route |
| `apps/web/src/client/App.tsx` | `DevModeBanner.tsx` | import + JSX mount | WIRED | Import line 13; used lines 67, 139 |
| `vite.config.ts` | `DevModeBanner.tsx` | `VITE_MOCK_SERVICES` env var (native Vite) | WIRED (override) | No define block; Vite auto-exposes VITE_* from .env.local — DevModeBanner reads it correctly |
| `scripts/seed.ts` | `api/_lib/db/client.ts` | `ensureDb()` | WIRED | Import at line 12; called at line 37 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|-------------------|--------|
| `DevModeBanner.tsx` | `import.meta.env.VITE_MOCK_SERVICES` | Vite env from `.env.local` | Yes (when VITE_MOCK_SERVICES=true in .env.local) | FLOWING (human-verifiable only) |
| `github-fixtures.ts` issues route | `buildIssueFixtures(owner, repo)` | Pure function, deterministic fixtures | Yes — 20 fixture objects | FLOWING |
| `seed.ts` | DB inserts | `faker.seed(12345)` + libsql execute | Yes — parameterized SQL inserts | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| vite.config.ts scripts project | `grep "name: 'scripts'" /Users/ivanfoong/repos/dxd/Currents/apps/web/vite.config.ts` | Matches line 52 | PASS |
| client.ts MOCK_SERVICES gate | `grep "MOCK_SERVICES === 'true'" /Users/ivanfoong/repos/dxd/Currents/apps/web/api/_lib/db/client.ts` | Matches line 8 | PASS |
| seed.ts process.exit guard | `grep "process.exit(1)" /Users/ivanfoong/repos/dxd/Currents/apps/web/scripts/seed.ts` | Matches lines 8, 116 | PASS |
| seed.ts faker.seed | `grep "faker.seed(12345)" /Users/ivanfoong/repos/dxd/Currents/apps/web/scripts/seed.ts` | Matches line 15 | PASS |
| dev-server.ts mock injection before collectRoutes | Lines 57–62 vs line 64 | Mock block at 57; collectRoutes at 64 | PASS |
| @faker-js/faker devDependency | `grep "@faker-js/faker" apps/web/package.json` | `"@faker-js/faker": "^10.4.0"` | PASS |
| CLAUDE.md principle 5 | `grep "5. Mocked Services" CLAUDE.md` | Line 546 | PASS |
| .env.example production warning | `grep "NEVER set" apps/web/.env.example` | Line 23 | PASS |
| DevModeBanner null guard | `grep "VITE_MOCK_SERVICES !== 'true'" DevModeBanner.tsx` | Line 2 | PASS |
| App.tsx mounts DevModeBanner | `grep "DevModeBanner" App.tsx` | Lines 13, 67, 139 | PASS |
| Browser rendering (amber banner) | Visual inspection | Requires browser + Vite running | SKIP |
| Mock OAuth redirect | Browser + dev-server | Requires full stack | SKIP |

### Requirements Coverage

No requirement IDs were declared in the plan frontmatter (all plans have `requirements: []`). The ROADMAP states "Requirements: TBD (cross-cutting dev tooling)". No REQUIREMENTS.md entries to cross-reference for this phase.

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `github-fixtures.ts` line 107 | `console.error` in mock OAuth callback | Info | Dev-only code; acceptable in mock context |
| `dev-server.ts` lines 83, 91 | `(target as any)[prop]` and `console.error` | Info | Dev-only server script; not production code |

No blockers found. No placeholder returns or stub implementations. All anti-pattern matches are in dev-only tooling scripts (not production `api/` handlers).

### Human Verification Required

#### 1. Amber Dev Mode Banner

**Test:** Set `VITE_MOCK_SERVICES=true` in `apps/web/.env.local`, start `npx vite` from `apps/web/`, visit http://localhost:5173
**Expected:** Amber "Dev Mode" badge visible in the fixed top-right corner on both the login page and after authentication
**Why human:** Visual CSS rendering; import.meta.env resolution at Vite build time cannot be confirmed without running Vite

#### 2. Mock OAuth Bypass (One-Click Login)

**Test:** With MOCK_SERVICES=true and the dev-server running (`npx tsx scripts/dev-server.ts`), click the GitHub sign-in button on the login page
**Expected:** Browser redirects to `/?token=<jwt>` immediately without visiting github.com; user is logged in as "Dev Admin"
**Why human:** Requires live browser session and full stack (dev-server + Vite); JWT redirect flow cannot be verified by grep

#### 3. Mock Issues Load in /issues

**Test:** After seeding and logging in via mock OAuth, navigate to /issues in the browser
**Expected:** Issues list shows fixture titles (e.g., "Fix null pointer exception in auth flow") loaded from the mock Express middleware — not an error state
**Why human:** Requires browser + authenticated session + dev-server returning PaginatedIssuesResult shape

#### 4. seed.ts Runs Without Error

**Test:** With MOCK_SERVICES=true in `apps/web/.env.local`, run `cd apps/web && npx tsx scripts/seed.ts`
**Expected:** Exits 0; console shows: `[seed] Created admin user: dev-admin@github.invalid`, `[seed] Created product: project-alpha/beta/gamma`, `[seed] Created 8 tasks for product: ...`, `[seed] Done.`
**Why human:** Requires local file system and correct .env.local setup; best verified in developer's own environment

### Gaps Summary

No automated gaps found. All code-verifiable must-haves pass. The one deviation from the plan (vite.config.ts define block) was intentional and self-correcting — Vite's native VITE_* env handling achieves the same functional outcome. The override is logged above.

The remaining 4 items require human smoke-test verification (visual UI, live server behavior). These are the same items listed as `checkpoint:human-verify` in Plan 05.

---

_Verified: 2026-04-22T12:00:00Z_
_Verifier: Claude (gsd-verifier)_
