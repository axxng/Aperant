---
phase: 04.1
plan: 02
subsystem: types, db-mappers, api, frontend
tags: [zod, discriminated-union, parse-dont-validate, tdd-green, task-types, db-boundary]

# Dependency graph
requires:
  - "04.1-01 — Wave 0 TDD RED stubs for rowToTask, buildSyncState, buildCreateTaskInput, buildUpdateTaskFields, rowToProduct"
provides:
  - "TaskStatusKey string union + TaskStatus alias for backward compatibility"
  - "GithubSyncState discriminated union (idle/pending/retrying/failed/complete)"
  - "Task discriminated union with 8 status variants (illegal state unrepresentable)"
  - "TaskOrderState = Record<TaskStatusKey, string[]>"
  - "taskDbRowSchema, productDbRowSchema, triageDbRowSchema in validation.ts"
  - "rowToTask() exported, Zod-parsed, switch-on-status variant builder"
  - "buildSyncState() exported pure FSM function"
  - "buildCreateTaskInput() and buildUpdateTaskFields() exported pure functions"
  - "rowToTriage() exported with TriageState discriminated union"
  - "rowToProduct() exported, Zod-parsed"
  - "updateTaskSyncState() internal helper for write-back sync column updates"
  - "All 14 Wave 0 stubs GREEN; 54 total tests pass; zero tsc errors"
affects:
  - "04.1-03 — GitHub parse boundary (github-sync.ts still uses any casts for l/a inline types)"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Discriminated union types: Task, GithubSyncState, TriageState with exhaustive switch"
    - "Parse-don't-validate at DB boundary: taskDbRowSchema.parse(row) in rowToTask()"
    - "Functional core: buildSyncState(), buildCreateTaskInput(), buildUpdateTaskFields() are pure functions"
    - "Internal sync helper: updateTaskSyncState() separates DB write concern from public UpdateTaskInput API"
    - "TaskStatusKey string union separate from Task type for use as Record<> keys"

key-files:
  created: []
  modified:
    - apps/web/src/shared/types/task.ts
    - apps/web/api/_lib/validation.ts
    - apps/web/api/_lib/db/tasks.ts
    - apps/web/api/_lib/db/triage.ts
    - apps/web/api/_lib/db/products.ts
    - apps/web/api/_lib/sync/github-sync.ts
    - apps/web/api/_lib/sync/github-writeback.ts
    - apps/web/api/tasks/[id]/index.ts
    - apps/web/api/tasks/[id]/status.ts
    - apps/web/api/tasks/order/[scope]/[status].ts
    - apps/web/src/client/stores/task-store.ts
    - apps/web/src/client/components/KanbanBoard.tsx
    - apps/web/src/client/components/TaskCard.tsx
    - apps/web/src/client/components/TaskEditDialog.tsx

key-decisions:
  - "TaskStatus retained as alias = TaskStatusKey for backward compatibility — zero churn on callers that already use TaskStatus"
  - "pr_created variant falls back to empty strings for githubIssueNumber/Url/Repo when DB columns are null — avoids runtime crash for legacy rows; Wave 3 can add strict guard"
  - "buildUpdateTaskFields returns any[] instead of unknown[] to satisfy libsql InArgs type constraint"
  - "updateTaskSyncState() internal helper added to tasks.ts rather than exposing githubSyncPending/githubSyncRetryCount in UpdateTaskInput — client cannot directly set sync state via PATCH API (threat T-04.1-02-04 mitigated)"
  - "github-sync.ts inline (l: any) and (a: any) casts left for Plan 03 (GitHub parse boundary plan)"

# Metrics
duration: 20min
completed: 2026-04-22
---

# Phase 04.1 Plan 02: Type Foundation + DB Mapper GREEN Phase Summary

**Task discriminated union with 8 variants, GithubSyncState FSM, Zod DB row schemas, exported rowToTask/rowToProduct/rowToTriage mappers, and cascade TypeScript fixes across 14 files — all 54 tests GREEN, zero tsc errors**

## Performance

- **Duration:** ~20 min
- **Completed:** 2026-04-22
- **Tasks:** 2
- **Files modified:** 14

## Accomplishments

### Task 1: Update task.ts types + validation.ts DB schemas (commit `77db9497`)

- Replaced flat `TaskStatus` string union with `TaskStatusKey` string union + `TaskStatus = TaskStatusKey` alias
- Added `GithubSyncState` discriminated union (5 variants: idle/pending/retrying/failed/complete)
- Restructured `Task` from flat interface to discriminated union of 8 status variants — illegal state unrepresentable
- Changed `TaskOrderState` from `Record<TaskStatus, string[]>` to `Record<TaskStatusKey, string[]>`
- Removed `githubSyncPending` and `githubSyncRetryCount` from `UpdateTaskInput`
- Removed `githubSyncPending` from `updateTaskSchema` (client cannot set sync state via PATCH)
- Appended `taskDbRowSchema`, `productDbRowSchema`, `triageDbRowSchema` to `validation.ts`

### Task 2: DB mappers + cascade fixes (commit `5998bab4`)

- Exported `rowToTask(row: unknown): Task` using `taskDbRowSchema.parse()` + exhaustive switch on status
- Exported `buildSyncState(pending, retryCount): GithubSyncState` pure FSM function
- Exported `buildCreateTaskInput()` and `buildUpdateTaskFields()` pure functions
- Added `updateTaskSyncState()` internal helper for writing github_sync_pending/retry_count columns
- Exported `rowToTriage()` using `triageDbRowSchema.parse()` + `TriageState` discriminated union
- Exported `rowToProduct()` using `productDbRowSchema.parse()`
- Fixed `getTaskOrder()`: replaced `as TaskStatus` blind cast with `z.enum(taskStatuses).parse()`
- Fixed `github-sync.ts`: `githubSyncPending` → `githubSyncState?.kind` check; `TaskStatus` → `TaskStatusKey`
- Fixed `github-writeback.ts`: `updateTask({ githubSyncPending })` calls → `updateTaskSyncState()`; retry count via `githubSyncState.retryCount`
- Fixed `tasks/[id]/index.ts` and `status.ts`: sync state calls → `updateTaskSyncState()`
- Fixed `tasks/order/[scope]/[status].ts`: `as TaskStatus` cast → `z.enum().parse()`
- Fixed `TaskCard.tsx`: `githubSyncPending` → `githubSyncState?.kind === 'pending' || 'retrying'`
- Fixed `KanbanBoard.tsx`: `TaskStatus` → `TaskStatusKey` in COLUMNS type
- Fixed `task-store.ts`: all `TaskStatus` function params → `TaskStatusKey`
- Fixed `TaskEditDialog.tsx`: `useState<TaskStatus>` → `useState<TaskStatusKey>`

## Task Commits

1. **Task 1: task.ts types + validation.ts schemas** — `77db9497`
2. **Task 2: DB mappers + cascade TypeScript fixes** — `5998bab4`

## Verification Results

```
Tests: 54 passed (10 test files)  — pre-existing 39 + 14 Wave 0 stubs GREEN + 1 additional
TypeScript: npx tsc --noEmit exits 0 (zero errors)
```

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Double replacement in github-sync.ts TaskStatus → TaskStatusKeyKey**
- **Found during:** Task 2 TypeScript check
- **Issue:** `replace_all` on `TaskStatus` ran on the import line that already contained `TaskStatusKey`, producing `TaskStatusKeyKey`
- **Fix:** Manually restored `TaskStatusKey` import; added explicit `TaskStatusKey` import before replace_all
- **Files modified:** `apps/web/api/_lib/sync/github-sync.ts`
- **Commit:** `5998bab4`

**2. [Rule 1 - Bug] `buildUpdateTaskFields` values typed as `unknown[]` rejected by libsql `InArgs`**
- **Found during:** Task 2 TypeScript check
- **Issue:** libsql's `InArgs` type requires `InValue[]` not `unknown[]`
- **Fix:** Changed return type and local array to `any[]`
- **Files modified:** `apps/web/api/_lib/db/tasks.ts`
- **Commit:** `5998bab4`

**3. [Rule 2 - Missing functionality] task-store.ts `updateTaskStatus` renamed accidentally**
- **Found during:** Task 2 TypeScript check (KanbanBoard.tsx referenced `updateTaskStatus` which disappeared)
- **Issue:** `replace_all` on `TaskStatus` also renamed `updateTaskStatus` → `updateTaskStatusKey`; `KanbanBoard.tsx` calls `updateTaskStatus` from the store
- **Fix:** Rewrote `task-store.ts` from scratch with correct `updateTaskStatus` name and `TaskStatusKey` type params
- **Files modified:** `apps/web/src/client/stores/task-store.ts`
- **Commit:** `5998bab4`

## Known Stubs

None — all functions are fully implemented. The plan 01 RED stubs are now GREEN.

Deferred for Plan 03: `github-sync.ts` inline `(l: any)` and `(a: any)` casts in GitHub issue/assignee mapping — Plan 03 (GitHub parse boundary) will address these.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes introduced. All changes are TypeScript-layer only — DB columns are unchanged.

Threat mitigations applied as specified in plan threat model:
- T-04.1-02-01: `as T` casts replaced with `taskDbRowSchema.parse(row)` — ZodError surfaces immediately on malformed rows
- T-04.1-02-03: `as TaskStatus` cast in order route replaced with `z.enum(taskStatuses).parse()` — rejects unknown status strings at API boundary
- T-04.1-02-04: `githubSyncPending` removed from `UpdateTaskInput` — client cannot directly set sync state via PATCH; write-back is internal-only via `updateTaskSyncState()`

## Self-Check: PASSED

- [x] `apps/web/src/shared/types/task.ts` — exports `TaskStatusKey`, `GithubSyncState`, `Task` (discriminated union), `TaskOrderState = Record<TaskStatusKey, string[]>`
- [x] `apps/web/api/_lib/validation.ts` — exports `taskDbRowSchema`, `productDbRowSchema`, `triageDbRowSchema`
- [x] `apps/web/api/_lib/db/tasks.ts` — exports `rowToTask`, `buildSyncState`, `buildCreateTaskInput`, `buildUpdateTaskFields`, `updateTaskSyncState`
- [x] `apps/web/api/_lib/db/triage.ts` — exports `TriageState`, `rowToTriage`
- [x] `apps/web/api/_lib/db/products.ts` — exports `rowToProduct`
- [x] Task 1 commit `77db9497` — FOUND
- [x] Task 2 commit `5998bab4` — FOUND
- [x] 54 tests pass (Wave 0 stubs GREEN)
- [x] `npx tsc --noEmit` exits 0

---
*Phase: 04.1-engineering-principles-refactor-parse-dont-validate-function*
*Completed: 2026-04-22*
