---
phase: "04.2"
plan: "05"
subsystem: "frontend / dx"
tags: ["dev-mode", "vite", "env-vars", "claude-md"]
key-files:
  created:
    - apps/web/src/client/components/DevModeBanner.tsx
  modified:
    - apps/web/vite.config.ts
    - apps/web/src/client/App.tsx
    - apps/web/.env.example
    - CLAUDE.md
metrics:
  tasks: 2/2
  tests: 77/77
---

## Summary

Plan 04.2-05 wired up the developer experience layer for mock mode:

1. **vite.config.ts define block** — `MOCK_SERVICES` env var exposed to the browser bundle as `import.meta.env.VITE_MOCK_SERVICES` at build time, safely defaulting to `'false'` in production.

2. **DevModeBanner component** — amber fixed top-right banner (`bg-amber-400 text-amber-900`) renders only when `VITE_MOCK_SERVICES === 'true'`, returns null otherwise. Mounted in `AuthenticatedApp` after `<ToastContainer />`.

3. **.env.example** — documented `MOCK_SERVICES`, `VITE_MOCK_SERVICES`, `JWT_SECRET` requirement, and a bold production warning ("NEVER set MOCK_SERVICES=true in production").

4. **CLAUDE.md engineering principle 5** — "Mocked Services for Dev Env" section added with: setup steps, service table (DB/GitHub API/OAuth), CORRECT vs WRONG code examples for `getClient()`, and guidance for future phases.

## Commits

| Task | Commit | Description |
|------|--------|-------------|
| 1+2  | `2d1c8a3c` | feat(04.2-05): add DevModeBanner, VITE_MOCK_SERVICES define, env docs, CLAUDE.md principle 5 |

## Deviations

None.

## Self-Check: PASSED

- [x] vite.config.ts contains `'import.meta.env.VITE_MOCK_SERVICES': JSON.stringify(process.env.MOCK_SERVICES ?? 'false')`
- [x] DevModeBanner.tsx guards with `if (import.meta.env.VITE_MOCK_SERVICES !== 'true') return null`
- [x] App.tsx imports and mounts `<DevModeBanner />`
- [x] .env.example contains MOCK_SERVICES=, VITE_MOCK_SERVICES=, and production warning
- [x] CLAUDE.md has `### 5. Mocked Services for Dev Env` with full documentation
- [x] 77/77 tests passing — no regressions
- [x] Checkpoint: human smoke test required (see plan task 3)
