---
context: phase
phase: 07-to-use-github-oauth-login-to-use-the-given-repo-permissions-
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-04-22T15:44:49Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Stale seed data | seed.ts used `'Needs review'` for review_reason, which fails taskDbRowSchema enum validation, causing rowToTask ZodError and silent loadTasks failure | advisory | Always check seeded values match Zod schema enums before testing |

<current_state>
Phase 07, Plan 07-04 (human verification). Automated gate (Task 1) is complete — all 139 tests pass, typecheck clean. Human smoke test (Task 2) is in progress. Three bugs were found and fixed during smoke testing:
1. Missing `exp` field in comment.test.ts mock TokenPayload
2. Missing `vite-env.d.ts` for `import.meta.env` support in DevModeBanner
3. `promoteMutation.onSuccess` didn't call `loadTasks(productId)` → Kanban didn't update after promotion
4. seed.ts used `'Needs review'` as review_reason value which fails schema enum validation → entire loadTasks silently fails

User is still performing smoke test. The last reported failure was "no tasks in Backlog column" which was caused by the invalid seed data. User needs to re-seed dev.db and retry.
</current_state>

<completed_work>

- Task 1: Run full automated suite gate — DONE (139 tests pass, typecheck clean)
- Task 2: Human smoke test — IN PROGRESS
  - Smoke test 1 step 1–9: Promote button visible, success toast, badge shown ✓
  - Smoke test 1 step 10: FAILED — fixed seed.ts invalid review_reason + added loadTasks(productId) on promote success
  - Awaiting user to re-seed and re-run smoke test
</completed_work>

<remaining_work>

- Task 2 (Human smoke test) — awaiting user verification after re-seed:
  - Re-seed: `cd apps/web && npx tsx scripts/seed.ts`
  - Smoke test 1 — Happy path (PROMOTE-01, PROMOTE-04): promote issue, confirm task in Kanban backlog
  - Smoke test 2 — Duplicate guard (PROMOTE-05): verify badge persists
  - Smoke test 3 — Write-back (PROMOTE-02): edit task, verify GitHub issue title updates
  - Smoke test 4 — i18n: check fr/issues.json has promote keys
- After user types "approved": create 07-04-SUMMARY.md, run code review gate, verify phase, update roadmap, mark complete
</remaining_work>

<decisions_made>

- Fixed `promoteMutation.onSuccess` to call `loadTasks(productId)` so Kanban refreshes immediately after promotion
- Fixed seed.ts: `'Needs review'` → `'errors'` (valid enum value per taskDbRowSchema)
- Added `vite-env.d.ts` with `/// <reference types="vite/client" />` to fix `import.meta.env` TypeScript error
- Fixed comment.test.ts mock user: added `exp` field to satisfy `TokenPayload` interface
</decisions_made>

<blockers>
- User must re-seed dev.db before smoke test will work: `cd apps/web && npx tsx scripts/seed.ts`
- Smoke test 3 (write-back) may not work in mock mode since mock GitHub API doesn't process real writes
</blockers>

## Required Reading (in order)
1. `.planning/phases/07-to-use-github-oauth-login-to-use-the-given-repo-permissions-/07-04-PLAN.md` — plan tasks and acceptance criteria
2. `.planning/phases/07-to-use-github-oauth-login-to-use-the-given-repo-permissions-/07-03-SUMMARY.md` — what was built in wave 2

## Infrastructure State
- Dev server: `cd apps/web && npx tsx scripts/dev-server.ts` (port 3001)
- Vite frontend: `npm run dev` (port 5173)
- MOCK_SERVICES=true required in .env.local
- dev.db needs re-seeding with `npx tsx scripts/seed.ts`

<context>
Executing phase 07, plan 04 (human verification wave). Automated suite passed. Three bugs fixed during smoke test. The critical one was seed.ts inserting `review_reason = 'Needs review'` which isn't a valid enum value in taskDbRowSchema — this caused rowToTask to throw ZodError on every task load, making the Kanban silently show nothing. User needs to re-seed and retry smoke test. Once user approves, need to create SUMMARY.md and complete phase verification flow.
</context>

<next_action>
Wait for user to re-seed (`cd apps/web && npx tsx scripts/seed.ts`) and retry smoke test. When user says "approved", create 07-04-SUMMARY.md and run phase verification/completion flow.
</next_action>
