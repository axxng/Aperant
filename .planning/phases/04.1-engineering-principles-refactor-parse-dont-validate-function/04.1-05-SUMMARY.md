---
phase: "04.1"
plan: "05"
subsystem: "claude-md-engineering-standards"
tags: ["parse-dont-validate", "functional-core", "fsm", "tdd", "documentation", "standards"]
dependency_graph:
  requires: ["04.1-04"]
  provides: ["Engineering Principles (apps/web/) section in CLAUDE.md"]
  affects:
    - "CLAUDE.md"
tech_stack:
  added: []
  patterns:
    - "CLAUDE.md as canonical standards location for all future phases"
    - "4-step handler shape documented with code examples"
    - "discriminated union inventory with current codebase references"
    - "DB mock pattern and handler mock pattern documented for TDD"
key_files:
  created: []
  modified:
    - "CLAUDE.md"
decisions:
  - "Engineering principles section appended at end of CLAUDE.md after existing content — preserves all existing guidance"
  - "Section scoped to apps/web/ explicitly — does not override desktop app patterns"
  - "4-step shape exceptions (cron/sync.ts, auth/github.ts, auth/me.ts) documented inline to prevent future over-application"
metrics:
  duration: "~2min"
  completed: "2026-04-22"
  tasks_completed: 1
  files_modified: 1
---

# Phase 04.1 Plan 05: Add Engineering Principles Section to CLAUDE.md Summary

**One-liner:** Engineering principles section appended to root CLAUDE.md with all 4 standards (parse-don't-validate, functional core/imperative shell, FSM, red-green TDD) and code examples derived from the Phase 04.1 refactored codebase.

## What Was Built

### CLAUDE.md `## Engineering Principles (apps/web/)` Section

Appended 185 lines to root `CLAUDE.md` covering all four principles with concrete examples cross-referenced directly to the refactored source files:

**1. Parse-Don't-Validate**
- Documents three boundary types: user input (safeParse+400), DB rows (parse→500), external API (parse→500)
- Before/after code examples for DB row mapping and GitHub API response parsing
- Points to `api/_lib/validation.ts` as the canonical schema registry

**2. Functional Core / Imperative Shell**
- 4-step handler shape (parse → authorize → pure → respond) with full working code example
- Documents the `// === Pure domain logic ===` comment separator convention
- Lists the three handler exceptions to the 4-step shape (cron, OAuth state init, me endpoint)
- Points to `buildCreateTaskInput()` / `buildUpdateTaskFields()` in `tasks.ts` as DB pure function pattern

**3. FSM / Illegal State Elimination**
- Correct vs wrong discriminated union examples
- Full inventory of current discriminated unions: Task (8 variants), GithubSyncState, TriageState, ProductSource
- Documents `TaskStatusKey` pattern for `Record<>` keys
- Guidance for adding new domain types with lifecycle

**4. Red-Green TDD**
- DB mock pattern from `triage.test.ts` (the `vi.mock('./client.js')` pattern)
- Handler mock pattern from `callback.test.ts` (`mockVercelReq`/`mockVercelRes` factories)
- Documents which pure functions are exported for testability
- "Going forward (Phase 5+)" enforcement note

## Test Results

- 68 tests GREEN (no change from doc-only modification)
- All pre-commit hooks passed

## Deviations from Plan

None — plan executed exactly as written. Section appended verbatim per plan specification with cross-references verified against actual source files.

## Known Stubs

None — this is a documentation-only plan.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes introduced. CLAUDE.md documents patterns already implemented in the codebase — no new trust boundaries.

T-04.1-05-01 (Tampering — incorrect code examples) mitigated: all code examples cross-referenced against actual refactored source files before writing (triage.test.ts, callback.test.ts, task.ts confirmed).

T-04.1-05-02 (Information Disclosure) accepted: CLAUDE.md is a developer reference file, no credentials or keys documented.

## Commits

| Task | Commit | Description |
|------|--------|-------------|
| Task 1 | 59003096 | docs(04.1-05): add Engineering Principles (apps/web/) section to CLAUDE.md |

## Self-Check: PASSED
