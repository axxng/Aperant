---
phase: 08-rename-from-aperant-to-currents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/client/stores/auth-store.ts
  - apps/web/src/client/lib/api-client.ts
  - apps/web/src/client/styles/globals.css
  - apps/web/index.html
  - apps/web/package.json
autonomous: true
requirements:
  - RENAME-01
  - RENAME-02

must_haves:
  truths:
    - "The Zustand persist key in auth-store.ts is 'currents-auth'"
    - "The localStorage.getItem() call in api-client.ts reads 'currents-auth'"
    - "The package.json name is '@currents/web'"
    - "The HTML page title is 'Currents'"
    - "The CSS file comment header reads 'Currents - Design System'"
    - "No occurrence of 'aperant-auth' remains in apps/web/src/"
    - "No occurrence of 'Aperant' or '@aperant' remains in apps/web/src/ or apps/web/package.json"
  artifacts:
    - path: "apps/web/src/client/stores/auth-store.ts"
      provides: "Zustand persist key rename"
      contains: "name: 'currents-auth'"
    - path: "apps/web/src/client/lib/api-client.ts"
      provides: "localStorage key read rename"
      contains: "localStorage.getItem('currents-auth')"
    - path: "apps/web/package.json"
      provides: "Package name rename"
      contains: '"name": "@currents/web"'
    - path: "apps/web/index.html"
      provides: "HTML page title rename"
      contains: "<title>Currents</title>"
    - path: "apps/web/src/client/styles/globals.css"
      provides: "CSS comment rename"
      contains: "/* Currents - Design System */"
  key_links:
    - from: "apps/web/src/client/stores/auth-store.ts"
      to: "apps/web/src/client/lib/api-client.ts"
      via: "shared localStorage key string must be identical in both files"
      pattern: "currents-auth"
---

<objective>
Rename all Aperant code-layer occurrences to Currents in the apps/web/ codebase: the paired localStorage key (auth-store.ts + api-client.ts), the package name (package.json), the HTML page title (index.html), and the CSS design system comment (globals.css).

Purpose: The localStorage key pair is the only operationally sensitive change — if auth-store.ts and api-client.ts reference different key names, every API call will silently return 401. These five files are grouped together to enforce atomic update of the paired key.

Output: Five files updated; zero occurrences of 'aperant-auth', '@aperant/web', or 'Aperant' remaining in the modified files.
</objective>

<execution_context>
@/Users/ivanfoong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ivanfoong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ivanfoong/repos/dxd/Currents/.planning/PROJECT.md
@/Users/ivanfoong/repos/dxd/Currents/.planning/ROADMAP.md
@/Users/ivanfoong/repos/dxd/Currents/.planning/STATE.md
@/Users/ivanfoong/repos/dxd/Currents/.planning/phases/08-rename-from-aperant-to-currents/08-RESEARCH.md
@/Users/ivanfoong/repos/dxd/Currents/.planning/phases/08-rename-from-aperant-to-currents/08-PATTERNS.md

<interfaces>
<!-- Current state of affected files — executor reads these before editing -->

From apps/web/src/client/stores/auth-store.ts line 63-65:
```typescript
      },
      { name: 'aperant-auth' }
    ),
```

From apps/web/src/client/lib/api-client.ts lines 8-16:
```typescript
function getAuthToken(): string | null {
  try {
    const raw = localStorage.getItem('aperant-auth');
    if (!raw) return null;
    const parsed = JSON.parse(raw);
    return parsed?.state?.token ?? null;
  } catch {
    return null;
  }
}
```

From apps/web/package.json lines 1-3:
```json
{
  "name": "@aperant/web",
  "version": "0.1.0",
```

From apps/web/index.html line 6:
```html
    <title>Auto Claude — Multi-Product Backlog</title>
```

From apps/web/src/client/styles/globals.css line 4:
```css
/* Aperant Web - Design System */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename paired localStorage key in auth-store.ts and api-client.ts</name>
  <files>
    apps/web/src/client/stores/auth-store.ts
    apps/web/src/client/lib/api-client.ts
  </files>
  <read_first>
    - apps/web/src/client/stores/auth-store.ts (read entire file — verify line 64 contains `{ name: 'aperant-auth' }` before editing)
    - apps/web/src/client/lib/api-client.ts (read entire file — verify line 10 contains `localStorage.getItem('aperant-auth')` before editing)
  </read_first>
  <action>
    These two files share a magic string localStorage key and MUST be updated atomically. If they diverge, every API call will return 401.

    In apps/web/src/client/stores/auth-store.ts:
    - Find line 64: `      { name: 'aperant-auth' }`
    - Replace with: `      { name: 'currents-auth' }`

    In apps/web/src/client/lib/api-client.ts:
    - Find line 10: `    const raw = localStorage.getItem('aperant-auth');`
    - Replace with: `    const raw = localStorage.getItem('currents-auth');`

    KNOWN SIDE-EFFECT: Renaming this key will silently log out all currently-authenticated browser sessions on their next page load. The old `aperant-auth` key remains orphaned in localStorage but will never be read again. This is acceptable — JWT sessions are short-lived.

    Do NOT change anything else in either file. Do NOT introduce a shared constant — this is a rename-only task.
  </action>
  <verify>
    <automated>grep -rn "aperant-auth" /Users/ivanfoong/repos/dxd/Currents/apps/web/src/ && echo "FAIL: aperant-auth still present" || echo "PASS: no aperant-auth found"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/client/stores/auth-store.ts contains `{ name: 'currents-auth' }` (grep: `grep "currents-auth" apps/web/src/client/stores/auth-store.ts` returns a result)
    - apps/web/src/client/lib/api-client.ts contains `localStorage.getItem('currents-auth')` (grep: `grep "currents-auth" apps/web/src/client/lib/api-client.ts` returns a result)
    - `grep -rn "aperant-auth" apps/web/src/` returns zero results
  </acceptance_criteria>
  <done>Both files reference 'currents-auth'; zero occurrences of 'aperant-auth' in apps/web/src/</done>
</task>

<task type="auto">
  <name>Task 2: Rename package name, HTML title, and CSS comment</name>
  <files>
    apps/web/package.json
    apps/web/index.html
    apps/web/src/client/styles/globals.css
  </files>
  <read_first>
    - apps/web/package.json (read entire file — verify line 2 contains `"name": "@aperant/web"`)
    - apps/web/index.html (read entire file — verify line 6 contains `<title>Auto Claude — Multi-Product Backlog</title>`)
    - apps/web/src/client/styles/globals.css (read entire file — verify line 4 contains `/* Aperant Web - Design System */`)
  </read_first>
  <action>
    Three independent string substitutions in three files:

    1. apps/web/package.json — line 2:
       Change: `"name": "@aperant/web"`
       To:     `"name": "@currents/web"`
       (Only this one line changes. Do not modify any other field.)

    2. apps/web/index.html — line 6:
       Change: `    <title>Auto Claude — Multi-Product Backlog</title>`
       To:     `    <title>Currents</title>`
       (The existing title "Auto Claude — Multi-Product Backlog" is wrong on both counts: "Auto Claude" is the desktop app name, and "Multi-Product Backlog" is outdated. Replace with just `Currents`.)

    3. apps/web/src/client/styles/globals.css — line 4:
       Change: `/* Aperant Web - Design System */`
       To:     `/* Currents - Design System */`
       (Comment only; no runtime effect.)

    NOTE: Do NOT run npm install in this task. The lockfile regeneration happens in Plan 02, Task 3.
  </action>
  <verify>
    <automated>grep '"name"' /Users/ivanfoong/repos/dxd/Currents/apps/web/package.json && grep "title" /Users/ivanfoong/repos/dxd/Currents/apps/web/index.html && grep "Design System" /Users/ivanfoong/repos/dxd/Currents/apps/web/src/client/styles/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/package.json line 2 equals: `  "name": "@currents/web",` (grep: `grep '"name": "@currents/web"' apps/web/package.json` returns a result)
    - apps/web/index.html contains `<title>Currents</title>` (grep: `grep '<title>Currents</title>' apps/web/index.html` returns a result)
    - apps/web/src/client/styles/globals.css line 4 equals: `/* Currents - Design System */` (grep: `grep '\/\* Currents - Design System \*\/' apps/web/src/client/styles/globals.css` returns a result)
    - apps/web/index.html does NOT contain "Auto Claude" (grep: `grep "Auto Claude" apps/web/index.html` returns zero results)
    - apps/web/package.json does NOT contain "@aperant/web" (grep: `grep "@aperant/web" apps/web/package.json` returns zero results)
  </acceptance_criteria>
  <done>package.json name is "@currents/web", HTML title is "Currents", CSS comment is "Currents - Design System"</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new boundaries | This plan introduces zero new code paths, endpoints, or data flows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | localStorage key rename | accept | Renaming 'aperant-auth' → 'currents-auth' does NOT change the security model. The key stores the same Zustand-persisted JWT state. No new attack surface is introduced — the string literal change is purely cosmetic to the auth flow. Known side-effect (session logout) is documented and acceptable. |
| T-08-02 | Information Disclosure | package.json name field | accept | '@aperant/web' → '@currents/web' is a monorepo workspace name. Not deployed; not exposed to users. No credential or secret exposure. |
| T-08-03 | Spoofing | HTML page title | accept | Changing `<title>` from "Auto Claude — Multi-Product Backlog" to "Currents" is purely cosmetic. No auth bypass, injection point, or credential exposure. |

No new attack surfaces are introduced. No auth bypasses, injection points, or credential exposure created by any change in this plan.
</threat_model>

<verification>
After both tasks complete:

```bash
# No aperant-auth key remains
grep -rn "aperant-auth" apps/web/src/
# Expected: zero results

# No @aperant/web package name remains
grep '"name"' apps/web/package.json
# Expected: "@currents/web"

# HTML title correct
grep "title" apps/web/index.html
# Expected: <title>Currents</title>

# CSS comment correct
grep "Design System" apps/web/src/client/styles/globals.css
# Expected: /* Currents - Design System */

# Test suite still green (no regressions from these edits)
cd apps/web && npm test
# Expected: 143/143 passing
```
</verification>

<success_criteria>
- `grep -rn "aperant-auth" apps/web/src/` returns zero results
- `grep '"name"' apps/web/package.json` returns `"name": "@currents/web"`
- `grep "title" apps/web/index.html` returns `<title>Currents</title>`
- `grep "Design System" apps/web/src/client/styles/globals.css` returns `/* Currents - Design System */`
- `cd apps/web && npm test` passes 143/143
</success_criteria>

<output>
After completion, create `.planning/phases/08-rename-from-aperant-to-currents/08-01-SUMMARY.md`
</output>
