---
phase: 04.1-engineering-principles-refactor-parse-dont-validate-function
plan: 01
subsystem: testing
tags: [vitest, tdd, zod, tasks, products, discriminated-union]

# Dependency graph
requires: []
provides:
  - "Failing test stubs for rowToTask() Zod parse + discriminated union variants"
  - "Failing test stubs for buildSyncState() idle/pending/retrying/failed"
  - "Failing test stubs for buildCreateTaskInput() + buildUpdateTaskFields() pure functions"
  - "Failing test stubs for rowToProduct() Zod parse"
affects:
  - "04.1-02 — Wave 1 GREEN phase implements these functions to make stubs pass"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "TDD RED phase: co-located test files with vi.mock('./client.js') hoisted before import"
    - "makeRow() helper pattern for constructing minimal valid DB rows with overrides"
    - "Import-undefined-as-not-a-function used as RED indicator for functions not yet exported"

key-files:
  created:
    - apps/web/api/_lib/db/tasks.test.ts
    - apps/web/api/_lib/db/products.test.ts
  modified: []

key-decisions:
  - "Test 1 (rowToTask throws on unknown status) technically passes in RED phase because calling undefined throws TypeError — acceptable since GREEN phase requires ZodError specifically; Wave 1 must make it throw ZodError"
  - "rowToProduct Test 1 (throws on missing id) also passes in RED phase for same reason — Wave 1 must specifically throw ZodError"

patterns-established:
  - "RED phase pattern: import stubs from source module; functions not yet exported; calling them throws TypeError satisfying .toThrow() loosely; GREEN phase must make them throw ZodError specifically"
  - "makeRow() override helper: minimal valid DB row object with spread overrides, used across all rowToTask() tests"
  - "Separate describe blocks per function group: rowToTask, buildSyncState, buildCreateTaskInput, buildUpdateTaskFields"

requirements-completed:
  - D-01
  - D-04
  - D-06
  - D-07
  - D-08
  - D-09

# Metrics
duration: 2min
completed: 2026-04-22
---

# Phase 04.1 Plan 01: Wave 0 TDD Stubs Summary

**14 failing test stubs establishing TDD contract for rowToTask() discriminated union, buildSyncState() FSM, buildCreateTaskInput/buildUpdateTaskFields() pure functions, and rowToProduct() Zod parse**

## Performance

- **Duration:** 2 min
- **Started:** 2026-04-22T01:47:39Z
- **Completed:** 2026-04-22T01:49:45Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Created `tasks.test.ts` with 12 stub tests: 5 for `rowToTask()` discriminated union variants and Zod parse errors, 4 for `buildSyncState()` FSM transitions, 2 for `buildCreateTaskInput()` pure record assembly, 2 for `buildUpdateTaskFields()` field extraction
- Created `products.test.ts` with 2 stub tests for `rowToProduct()` Zod parse — required field missing throws, valid row maps correctly
- All 41 pre-existing tests continue to pass (8 test files green); only 2 new stub test files fail in RED phase

## Task Commits

Each task was committed atomically:

1. **Task 1: Create tasks.test.ts with 12 failing stubs** - `9432b1a6` (test)
2. **Task 2: Create products.test.ts with 2 failing stubs** - `146d22b0` (test)

**Plan metadata:** (docs commit to follow)

_Note: This is a TDD RED phase plan — all new tests intentionally fail. Wave 1 (Plan 02) implements GREEN._

## Files Created/Modified

- `apps/web/api/_lib/db/tasks.test.ts` — 12 failing stubs: rowToTask (5 tests), buildSyncState (4 tests), buildCreateTaskInput (2 tests), buildUpdateTaskFields (2 tests)
- `apps/web/api/_lib/db/products.test.ts` — 2 failing stubs: rowToProduct throws on missing id, rowToProduct maps valid row

## Decisions Made

- Two tests technically pass in RED phase (rowToTask throws + rowToProduct throws) because the functions are not exported so calling them throws TypeError, satisfying `.toThrow()` loosely. This is acceptable RED phase behavior. Wave 1 (GREEN phase) must make them throw ZodError specifically.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Wave 0 TDD contract is established: all 14 stubs define exact behavior Wave 1 must implement
- Plan 02 (Wave 1) implements `rowToTask()` with Zod discriminated union parse, `buildSyncState()`, `buildCreateTaskInput()`, `buildUpdateTaskFields()`, and `rowToProduct()` with Zod parse — making all 14 stubs green
- No blockers

## Known Stubs

None — these files ARE the stubs; they are the RED phase artifacts. The stubs reference functions not yet exported (`rowToTask`, `buildSyncState`, `buildCreateTaskInput`, `buildUpdateTaskFields`, `rowToProduct`).

## Self-Check: PASSED

- [x] `apps/web/api/_lib/db/tasks.test.ts` — FOUND
- [x] `apps/web/api/_lib/db/products.test.ts` — FOUND
- [x] Task 1 commit `9432b1a6` — FOUND
- [x] Task 2 commit `146d22b0` — FOUND

---
*Phase: 04.1-engineering-principles-refactor-parse-dont-validate-function*
*Completed: 2026-04-22*
