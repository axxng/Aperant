---
phase: 04.2-fake-data-and-mocked-services-for-dev-env
plan: "03"
subsystem: testing/dev-tooling

tags: [mock-services, github-fixtures, express-middleware, oauth-bypass, tdd, green]

requires:
  - phase: 04.2
    plan: "01"
    provides: Failing RED test stubs for MOCK-05 (github-fixtures.test.ts)

provides:
  - scripts/mocks/github-fixtures.ts — buildIssueFixtures, buildLabelFixtures, registerMockRoutes
  - Express mock middleware for GitHub API + OAuth bypass
  - dev-server.ts conditional mock injection before real route registration

affects:
  - 04.2-05 (documentation of MOCK_SERVICES env var)

tech-stack:
  added: []
  patterns:
    - "Express first-match routing: mock routes registered before real Vercel handlers"
    - "Dynamic import('./mocks/github-fixtures.js') in dev-server.ts for conditional loading"
    - "Mock OAuth bypass: upsertOAuthUser + createToken + redirect to /?token=<jwt>"
    - "Fixture data: 15 open + 5 closed issues, no pull_request field, all fields pass gitHubApiIssueSchema"

key-files:
  created:
    - apps/web/scripts/mocks/github-fixtures.ts
  modified:
    - apps/web/scripts/dev-server.ts

key-decisions:
  - "Route paths use Express colon syntax (:owner/:repo) NOT Vercel bracket syntax ([owner]/[repo])"
  - "Mock injection placed at TOP of main() before collectRoutes() — Express first-match ensures mock routes win"
  - "buildIssueFixtures and buildLabelFixtures exported as named functions (not nested in registerMockRoutes) for unit testability"
  - "LABELS constant defined at module level and reused by both buildLabelFixtures and buildIssueFixtures"

requirements-completed: []

duration: ~10min
completed: 2026-04-22
---

# Phase 04.2 Plan 03: GitHub Fixture Middleware Summary

**GitHub API fixture middleware and OAuth bypass for dev environment — MOCK-05 RED stubs turned GREEN with 20-issue fixture set and 4 Express mock routes wired into dev-server.ts**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-04-22T03:09:00Z
- **Completed:** 2026-04-22T03:19:51Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Created `apps/web/scripts/mocks/github-fixtures.ts` with three named exports:
  - `buildIssueFixtures(owner, repo)` — returns 20 issues (15 open + 5 closed), all fields pass `gitHubApiIssueSchema`, no `pull_request` field
  - `buildLabelFixtures()` — returns 5 label objects with id/name/color/description fields
  - `registerMockRoutes(app)` — registers 4 Express routes: OAuth initiate bypass, OAuth callback JWT issuer, issues endpoint, labels endpoint
- Modified `apps/web/scripts/dev-server.ts` to dynamically import and register mock routes before `collectRoutes()` when `MOCK_SERVICES=true`
- MOCK-05a (schema conformance), MOCK-05b (no pull_request), MOCK-05c (open+closed mix) — all pass GREEN

## Task Commits

1. **Task 1: Create scripts/mocks/github-fixtures.ts** — `d1d10c7d` (feat)
2. **Task 2: Inject mock middleware into dev-server.ts** — `a20f5b4f` (feat)

## Files Created/Modified

- `apps/web/scripts/mocks/github-fixtures.ts` — 137 lines; fixture builders + Express route registration
- `apps/web/scripts/dev-server.ts` — +7 lines; MOCK_SERVICES guard at top of main()

## Decisions Made

- Express colon syntax (`:owner/:repo`) used for route params — bracket syntax is Vercel-specific and breaks Express routing
- Mock injection placed at TOP of `main()` before `collectRoutes(API_DIR)` — Express first-match semantics mean registration order determines which handler wins; mocks must win
- `buildIssueFixtures` and `buildLabelFixtures` exported as top-level named functions so unit tests can import them directly without invoking Express

## Deviations from Plan

None — plan executed exactly as written.

## Threat Surface Scan

Per plan threat model (T-04.2-03-01): Mock OAuth callback issues JWT without real GitHub validation — guarded by `MOCK_SERVICES=true` in dev-server.ts. The route is never registered in production (Vercel env does not set MOCK_SERVICES). No new threat surfaces beyond what the plan's STRIDE register already covers.

## Known Stubs

None — `registerMockRoutes` is fully wired; fixture data is deterministic and complete.

## Self-Check

- [x] `apps/web/scripts/mocks/github-fixtures.ts` exists
- [x] File exports `buildIssueFixtures`, `buildLabelFixtures`, `registerMockRoutes`
- [x] File contains `:owner/:repo` colon syntax (no bracket syntax)
- [x] File imports `createToken` from `../../api/_lib/auth/jwt.js`
- [x] `apps/web/scripts/dev-server.ts` contains `MOCK_SERVICES === 'true'` guard
- [x] `apps/web/scripts/dev-server.ts` contains `import('./mocks/github-fixtures.js')`
- [x] Mock injection appears before `collectRoutes(API_DIR)` (line 58 vs line 64)
- [x] MOCK-05a, MOCK-05b, MOCK-05c all pass (vitest exits 0)
- [x] Commits `d1d10c7d` and `a20f5b4f` exist

## Self-Check: PASSED

---
*Phase: 04.2-fake-data-and-mocked-services-for-dev-env*
*Completed: 2026-04-22*
